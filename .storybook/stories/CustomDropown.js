import React from 'react';
import { storiesOf } from '@storybook/react-native';
import CustomDropdown from '../../components/CustomDropdown';

storiesOf('CustomDropdown', module).add('Default View', () => <CustomDropdown />);

export {};
